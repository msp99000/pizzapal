import 'package:flutter/material.dart';

class PizzaPal extends StatelessWidget {
  const PizzaPal({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
        title: "PizzaPal", home: Center(child: Text("Hunter")));
  }
}
